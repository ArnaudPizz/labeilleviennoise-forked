name: Tests
on: push
jobs:
  install-packages:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: npm install -g pnpm
  testing-code:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: pnmp test
